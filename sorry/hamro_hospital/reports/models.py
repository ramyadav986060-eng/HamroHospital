# The Reports app is read-only by design: it has no models of its own and
# instead aggregates data that already lives in patients, billing, pharmacy,
# laboratory, and accounts (AuditLog). See reports/views.py.
